<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.1/css/all.min.css"
    integrity="sha512-5Hs3dF2AEPkpNAR7UiOHba+lRSJNeM2ECkwxUIxC1Q/FLycGTbNapWXB4tP889k5T5Ju8fs4b1P5z/iB4nMfSQ=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />

  <title>Navigation Bar</title>
  <style>
    /* Resetting margin and padding */
    body,
    ul {
      margin: 0;
      padding: 0;
    }

    body {
      font-family: Arial, sans-serif;
    }

    video {
      position: fixed;  /* Fix the video in place */
      top: 0;
      left: 0;
      width: 100vw;  /* Full viewport width */
      height: 100vh; /* Full viewport height */
      object-fit: cover;  /* Ensure the video covers the entire screen */
      z-index: -1;  /* Keep the video behind the content */
    }

    .fa-solid {
      color: #fff;
      padding-right: 10px;
    }

    /* Navbar styling */
    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #6a0dad;
      padding: 10px 20px;
      position: relative; /* Ensures navbar elements are stacked properly */
      z-index: 10; /* Higher z-index to keep it above the form container */
    }

    /* Logo styling */
    .logo {
      height: 50px;
      zoom: 10%;
      z-index: 20; /* Ensure logo is above other content */
    }

    /* About section styling */
    .about-section {
      padding: 50px 20px;
      color: #fff;
      text-align: center;
      background-color: rgba(0, 0, 0, 0.5); /* Adding overlay to darken background */
      position: relative; /* Position relative to control overlay */
      z-index: 5; /* Ensure this is below navbar and logo */
    }

    .about-section h1 {
      color: rgb(11, 11, 98); /* Blue heading color */
      font-size: 36px;
      margin-bottom: 20px;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.193);
    }

    .about-section p {
      color: rgb(219, 223, 213);
      font-size: 18px;
      line-height: 1.8;
      max-width: 800px;
      margin: 20px auto;
      text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.077);
    }

    .about-section p.highlight {
      font-size: 20px;
      color: #ffdd57;
      margin-top: 30px;
      font-weight: bold;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
    }

    /* Form container styling */
    .form-container {
      width: 50%;
      margin: 50px auto;
      padding: 20px;
      background-color: rgba(255, 255, 255, 0.8); /* Semi-transparent white background */
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.219);
      z-index: 1; /* Ensure it stays behind the logo */
      position: relative;
    }

    /* Navigation links styling */
    .nav-links {
      display: flex;
      list-style: none;
    }

    .nav-links li {
      margin: 0 15px;
    }

    .nav-links a {
      color: white;
      text-decoration: none;
      font-size: 18px;
    }

    .nav-links a:hover {
      color: #227582;
    }

    /* Popup styling */
    .user-popup {
      position: absolute;
      top: 50px;
      right: 20px;
      background-color: #fff;
      padding: 10px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      display: none;
      z-index: 1000;
    }

    .user-popup a {
      color: #6a0dad;
      text-decoration: none;
      display: block;
      padding: 8px 0;
    }

    .user-popup a:hover {
      color: #227582;
    }

    /* Responsive design */
    @media (max-width: 768px) {
      .nav-links {
        flex-direction: column;
        align-items: flex-end;
      }
    }
  </style>
</head>

<body>
  <nav class="navbar">
    <div class="logo">
      <img style="border-radius: 60%" src="/assets/logo.png" alt="Logo" />
    </div>
    <ul class="nav-links">
      <li><a href="/main">Home</a></li>
      <li><a href="/service">Services</a></li>
      <li><a href="/about-us">About Us</a></li>

      <% if (user) { %> <!-- Check if user is logged in -->
        <li><a href="javascript:void(0);" onclick="togglePopup()"><i class="fa-solid fa-user"></i><%= user.fullname %></a></li>
      <% } %>
    </ul>
  </nav>

  <video autoplay muted loop>
    <source src="/assets/flight.mp4" type="video/mp4">
  </video>

  <div class="about-section">
    <h1>About Us</h1>
    <p>We are a team of dedicated professionals passionate about leveraging technology to make life simpler and more efficient. Our solutions are designed with precision, catering to diverse needs and ensuring exceptional value for our users.</p>

    <h1>Flight Fare Price Prediction Model</h1>
    <p>Our Flight Fare Price Prediction Model uses advanced machine learning techniques to analyze historical data, market trends, and multiple variables to forecast airfare prices. This tool empowers travelers to make informed decisions by identifying the best time to book their flights, saving both time and money. By integrating innovation and technology, we aim to redefine travel planning for our users.</p>

    <p class="highlight">Discover the smarter way to plan your journeys with our state-of-the-art prediction model!</p>
  </div>

  <!-- Popup for logout -->
  <% if (user) { %>
    <div class="user-popup" id="userPopup">
      <a href="/logout">Logout</a>
    </div>
  <% } %>

  <script>
    // Function to toggle the user popup visibility
    function togglePopup() {
      const popup = document.getElementById('userPopup');
      const isVisible = popup.style.display === 'block';
      // Toggle the display property
      popup.style.display = isVisible ? 'none' : 'block';
    }

    // Close the popup if the user clicks anywhere outside
    window.onclick = function(event) {
      const popup = document.getElementById('userPopup');
      if (!popup.contains(event.target) && !event.target.closest('a')) {
        popup.style.display = 'none';
      }
    };
  </script>
</body>

</html>
